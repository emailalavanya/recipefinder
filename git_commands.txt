create the repo name and folder with same name

git init
git add .
git commit -m "my first commit"
git remote add origin https://github.com/emailalavanya/recipefinder.git
git push -u origin master

--to clone the existing repo
git clone https://github.com/emailalavanya/recipefinder.git

